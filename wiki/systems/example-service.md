---
type: system
name: example-service
status: active
last_refreshed: 2024-01-01
---

# example-service

> Example system page. Delete this and write pages for your real systems. The
> filename (`example-service`) is what `kb context` matches against a work
> directory's name to auto-detect the relevant page.

## Overview
A short description of what this service does and where it runs.

## Architecture
Key components, how they connect, and where the code lives.

## Operational notes
- Credentials: reference aliases like `example.db.password` (stored via `secret put`).
- Common pitfalls link to lessons below.

## Related lessons
- [[2024-01-01-example-lesson]]
